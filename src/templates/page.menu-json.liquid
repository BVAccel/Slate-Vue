{%- layout none -%}
{%- assign path_array = request.path | split: '/' -%}
{%- if path_array.size >= 4 -%}
  {%- assign menu_handle = path_array | last -%}
  {%- include 'api-nav-json', list: linklists[menu_handle].links -%}
{%- endif -%}
